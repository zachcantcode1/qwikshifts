{
  "name": "api",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch index.ts",
    "start": "bun run index.ts",
    "build": "echo 'No build needed for Bun API'",
    "db:reset": "rm -f qwikshifts.sqlite* && echo 'Database reset complete'"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.8"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@qwikshifts/core": "workspace:*",
    "date-fns": "^4.1.0",
    "drizzle-orm": "^0.45.0",
    "hono": "^4.10.7",
    "zod": "^4.1.13"
  }
}
